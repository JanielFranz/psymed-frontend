import { BiologicalFunctions } from './biological-functions.entity';

describe('BiologicalFunctions', () => {
  it('should create an instance', () => {
    expect(new BiologicalFunctions()).toBeTruthy();
  });
});
