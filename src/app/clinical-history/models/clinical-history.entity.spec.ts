import { ClinicalHistory } from './clinical-history.entity';

describe('ClinicalHistoryEntity', () => {
  it('should create an instance', () => {
    expect(new ClinicalHistory()).toBeTruthy();
  });
});
