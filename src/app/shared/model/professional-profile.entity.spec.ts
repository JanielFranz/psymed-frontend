import { ProfessionalProfileEntity } from './professional-profile.entity';

describe('ProfessionalProfileEntity', () => {
  it('should create an instance', () => {
    expect(new ProfessionalProfileEntity()).toBeTruthy();
  });
});
