import { ProfessionalEntity } from './professional.entity';

describe('ProfessionalEntity', () => {
  it('should create an instance', () => {
    expect(new ProfessionalEntity()).toBeTruthy();
  });
});
